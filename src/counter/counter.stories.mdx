import { Meta, Props, Story, Canvas } from '@web/storybook-prebuilt/addon-docs/blocks.js';

<Meta title="Components/Counter" />

# Counter

Badges provide a method to highlight a count of an element either next to it
or inside the element itself.

## Installation

To use the counter component, import the component in your JavaScript.

```typescript
import 'lib/counter/register.js';
```

```html
<cda-counter></cda-counter>
```

## Counter

<Canvas>
  <Story id="stories-counter--basic" />
</Canvas>


## API

<Props of="cda-counter" />
